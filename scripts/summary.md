# scripts Summary

- Contains cross-platform convenience installers to set up the project quickly.
- `install.sh` targets macOS/Linux: installs Poetry, pulls repo dependencies, configures Playwright browsers, and fetches Camoufox data.
- `install.ps1` performs the analogous flow for Windows PowerShell users.
